#include <stdio.h>
int main(void)
{
	int t, n;
	int* e, r, ans;
	int i, j;
	int index;

	scanf("%d", &t);
	ans = (int*)malloc(sizeof(int) * t);

	for (i = 0; i < t; i++)
	{
		scanf("%d", &n);
		e = (int*)malloc(sizeof(int) * n);
		r = (int*)malloc(sizeof(int) * n);

		
	}
}